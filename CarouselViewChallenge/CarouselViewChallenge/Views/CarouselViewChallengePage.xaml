<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Visual="Material"
             x:Class="CarouselViewChallenge.Views.CarouselViewChallengePage"
             xmlns:vm="clr-namespace:CarouselViewChallenge.ViewModels">

    <StackLayout>
        <!--<Picker ItemsSource="{Binding StartingLetters}" Title="Select a starting letter" SelectedItem="{Binding SelectedLetter}"></Picker>
        <CarouselView ItemsSource="{Binding StartingLetters}" BackgroundColor="Green" HeightRequest="200" >
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <StackLayout BackgroundColor="CornflowerBlue">
                        <Frame BorderColor="Black" 
                               Margin="8" 
                               HasShadow="True" 
                               HeightRequest="150" 
                               VerticalOptions="CenterAndExpand">
                            <StackLayout>
                                <Label Text="{Binding}" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="White"
                                       HorizontalTextAlignment="Center"/>
                            </StackLayout>
                        </Frame>
                    </StackLayout>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>-->

        <CarouselView ItemsSource="{Binding Monsters}" BackgroundColor="Green" HeightRequest="200" >
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <StackLayout BackgroundColor="CornflowerBlue">
                        <Frame BorderColor="Black" 
                               Margin="8" 
                               HasShadow="True" 
                               HeightRequest="150" 
                               VerticalOptions="CenterAndExpand">
                            <StackLayout>
                                <Label Text="{Binding Name}" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="White"
                                       HorizontalTextAlignment="Center"/>
                            </StackLayout>
                        </Frame>
                    </StackLayout>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>

    </StackLayout>
</ContentPage>